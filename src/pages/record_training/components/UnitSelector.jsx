import React from "react";
import Select from "react-select";

const UnitSelector = () => {
  return (
    <div>
      <Select className="block w-full" options={[]} placeholder="Unit" />
    </div>
  );
};

export default UnitSelector;
